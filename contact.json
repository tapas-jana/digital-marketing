const fs = require("fs");

app.post("/contact", (req, res) => {
  const line = `"${new Date().toISOString()}","${req.body.name}","${req.body.email}","${req.body.message}"\n`;

  fs.appendFile("contacts.csv", line, err => {
    if (err) console.error(err);
  });

  res.redirect("/thank-you");
});

